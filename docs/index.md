# Deptool

Deptool is a declarative configuration deployment tool. It manages configuration
files on a cluster of Linux hosts. Deptool is designed for smallish clusters
(1–50 hosts) managed by a small group of operators (1–5 people). Deptool is:

 * **Declarative.** You define the desired state of your cluster, Deptool
   materializes that state. When you stop including a file, Deptool removes it.
   No more lingering files or explicit cleanup tasks.
 * **Daemonless.** Deptool requires no installation, enrolment, or daemons on
   the target host. The only prerequisite is the ability to open an
   <abbr>SSH</abbr> connection from the operator’s machine.
 * **Fast.** Deptool only connects to hosts that are affected by a change,
   which it can determine statically. It deploys against hosts in parallel,
   and merely pushes files and restarts systemd units.
   Updating a configuration file across the cluster can be done sub-second.
 * **Safe.** Deptool computes a deployment plan ahead of time. You can view the
   full diff before even connecting. It can roll back automatically in case a
   systemd unit fails to start after a deploy, and it prevents concurrent
   deploys with host-level locks.
 * **Simple and opinionated.** Deptool manages a directory of files and systemd
   units across a small cluster. This configuration needs to be written to use
   specific file paths. Deptool is not a generic tool that tries to solve every
   deployment problem for everybody.

Deptool is not:

 * **A full configuration management tool.** Deptool does not generate the files
   it deploys, it has no support for templating or control flow. Cluster
   configuration should be generated by a separate tool such as
   [<abbr>RCL</abbr>][rcl], Nix, or custom configure scripts.
 * **Bidirectional.** Deptool pushes configuration from the operator machine
   to target hosts. Aside from tracking the deployed version, Deptool does not
   transport information from target hosts back to the operator.

[rcl]: https://rcl-lang.org/

## How it works

Deptool stores configuration in its _store_: a Git repository with a two-tier
directory structure: top-level directories define _hosts_, second-level
directories define _apps_. For example:

    dns01
    └── nsd
        ├── example.com.zone
        ├── manifest.json
        ├── nsd.conf
        └── systemd
            └── nsd.service
    web01
    └── nginx
        ├── manifest.json
        ├── sites-enabled
        │   └── example.com.conf
        └── systemd
            └── nginx.service

Per host, Deptool materializes the app directories in `/var/lib/deptool/apps`.
It then manages systemd units, and it can create symlinks into `/var/lib/deptool`
on the filesystem as defined in the manifest, `manifest.json`. See also the
[directory layout](directory_layout.md) and [manifests](manifests.md) reference.

On the operator machine, Deptool keeps a remote-tracking ref per target host.
This way it knows what is deployed on that host, and whether a new commit
affects any of the host’s apps. This also enables it to show a full diff ahead
of time.

For a single operator deploying from a single store, the remote-tracking refs
are always up to date. This means Deptool never initiates any superfluous
connections. For multiple operators collaboratively managing a cluster, an
operator’s local refs may provide an outdated view of the cluster. Deptool
detects this before applying any changes: it locks every host with the intent
of applying the plan as accepted. If during locking it turns out the plan was
stale, Deptool fetches missing commits so it can re-plan with the latest
information. At a team size where this opportunistic deployment workflow starts
suffering from contention, Deptool is suitable for running centrally from a
single store, integrated into a GitOps pipeline.

## Acknowledgements

 * Deptool takes inspiration from Nix, in particular the idea of generating
   machine configuration ahead of time, storing it in a store where multiple
   versions can coexist, and atomically swapping symlinks to activate new versions.
 * Deptool takes inspiration from OpenTofu, in particular the idea of having a
   separate _plan_ and _apply_ phase, so that the operator can inspect and
   accept exactly what will be deployed before applying any destructive changes.
 * Deptool learns from Ansible’s many pitfalls.
