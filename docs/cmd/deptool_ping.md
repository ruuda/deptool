# deptool ping

    deptool ping [--limit <hosts>]... [--] [<dir>]

## Description

TODO(ruuda): describe what `deptool ping` does, when to use it, and how its
SSH-based round-trip differs from ICMP `ping`. Mention that `<dir>` defaults to
the last-used cluster (cross-reference deploy).

## Options

### `--limit <hosts>`

TODO(ruuda): describe `--limit` (same shape as in deploy and sync).

### `--store`

TODO(ruuda): describe `--store` (same shape as in deploy and sync).
