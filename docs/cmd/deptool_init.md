# deptool init

    deptool init

## Description

Initialize a new empty [store](../store.md). By default the store is located
at `.deptool` in the current directory. This can be overriden with
[`--store`](deptool.md#-store).
